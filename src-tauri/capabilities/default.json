{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the CyberDash shell window: read the current theme and subscribe to system theme changes.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-theme",
    "core:window:allow-set-theme"
  ]
}
