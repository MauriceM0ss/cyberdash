{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the CyberDash shell: read/subscribe to the system theme, and create, focus and close the native child webviews that embed each app. Positioning and visibility are NOT here — Tauri cannot place webviews on Linux, so the shell does that itself through the stage_place / stage_hide commands in src/stage.rs.",
  "windows": ["main"],
  "webviews": ["main", "app__*"],
  "permissions": [
    "core:default",
    "core:window:allow-theme",
    "core:window:allow-set-theme",
    "core:window:allow-scale-factor",
    "core:webview:allow-create-webview",
    "core:webview:allow-set-webview-focus",
    "core:webview:allow-webview-close"
  ]
}
